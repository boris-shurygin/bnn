"""Эксперименты. Каждый модуль определяет `run(config, ctx) -> dict`.

До блока 3 дублирование между модулями допустимо и даже полезно: код уезжает
в `src/bioplast/` только когда одно и то же написано трижды (CLAUDE.md).
"""
